create database your_databse
use yourdatabase

CREATE TABLE stock (
	kode_barang int PRIMARY KEY NOT NULL,
    nama_barang VARCHAR(40) ,
    stok_barang INT
    harga int
);

create table admin (
	username varchar(10)
	password varchar(8)
    
)

